"""Phase 7 — evaluation. Localization eval: given a change description, does
retrieval surface the files that actually changed? Built self-supervised from
the ingested commit history (SWE-bench-flavored)."""
